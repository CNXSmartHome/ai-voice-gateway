# Mobile App (VG-008)

React Native application for the AI Voice Gateway.

**Status:** reserved. Not yet initialized — no source, no `package.json`, and
deliberately not part of the npm workspace, so `npm install` at the repo root
stays fast until this app exists.

## Planned scope
| Task | Description |
| --- | --- |
| VG-007 | BLE Wi-Fi provisioning skeleton (app side) |
| VG-008 | Add-gateway flow |
| VG-013 | Room assignment |
| VG-036 | UI cleanup |

Initialization happens in VG-008, which adds this directory to the
`workspaces` array in the root `package.json`.
